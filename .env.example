# API Configuration
API_SECRET_TOKEN=your-secret-token-here
WEBHOOK_TOKEN=your-webhook-token-here

# Kommo CRM Configuration
KOMMO_SUBDOMAIN=your-kommo-subdomain
KOMMO_ACCESS_TOKEN=your-kommo-access-token

# Email Configuration (Resend)
RESEND_API_KEY=your-resend-api-key-here
FROM_EMAIL=invoices@yourdomain.com

# MongoDB Configuration
# Note: Railway will automatically provide these variables when you add a MongoDB service
MONGO_INITDB_ROOT_USERNAME=mongo
MONGO_INITDB_ROOT_PASSWORD=your-mongodb-password

# MongoDB Connection URLs
# For Railway deployment (Railway provides these variables automatically):
# MONGO_URL will be constructed from Railway's environment variables
# Use RAILWAY_PRIVATE_DOMAIN for internal communication (more secure and faster)

# For local development:
MONGO_URL=mongodb://mongo:your-mongodb-password@localhost:27017

# Individual MongoDB connection components (fallback)
MONGOHOST=localhost
MONGOPORT=27017
MONGOUSER=mongo
MONGOPASSWORD=your-mongodb-password

# Railway-specific variables (automatically provided by Railway):
# RAILWAY_PRIVATE_DOMAIN - internal network domain for MongoDB
# RAILWAY_TCP_PROXY_DOMAIN - external TCP proxy domain
# RAILWAY_TCP_PROXY_PORT - external TCP proxy port

